import { NavigationContainer } from "@react-navigation/native";

import { AplicationRoutes } from "./app.routes";

export function Routes() {
    return (
        <NavigationContainer>
            <AplicationRoutes />
        </NavigationContainer>)
}